<%= form_for([@question, @answer]) do |f| %>
	<%= render 'common/form_errors', object: @answer%>

	<p>
		<%= f.label :body, "Answer" %>
		<%= f.text_area :body %>
		<%= f.button "Post Answer", class: "btn btn-success" %>
	</p>
<% end %>