<%= form_for(@question) do |f| %>
	<%= render "common/form_errors", object: @question %>
	<p>
		<%= f.label :body, "Question" %><br >
		<%= f.text_area :body, placeholder: "Ask your question here..." %> 
		<%= f.button "Ask a question", class: "btn btn-primary" %> 
	</p>
<% end %>